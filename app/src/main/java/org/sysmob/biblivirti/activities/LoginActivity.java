package org.sysmob.biblivirti.activities;

import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;
import android.view.View;
import android.widget.Toast;

import org.sysmob.biblivirti.R;

public class LoginActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_login);

    }

    public void clickRecuperarSenha(View view) {
        Toast.makeText(this, "Esta funcionalidade ainda não foi implementacada!", Toast.LENGTH_SHORT).show();
    }

    public void clickNovaConta(View view) {
        Toast.makeText(this, "Esta funcionalidade ainda não foi implementacada!", Toast.LENGTH_SHORT).show();
    }

    public void clickFacebookLogin(View view) {
        Toast.makeText(this, "Esta funcionalidade ainda não foi implementacada!", Toast.LENGTH_SHORT).show();
    }

    public void clickLogin(View view) {
        Toast.makeText(this, "Esta funcionalidade ainda não foi implementacada!", Toast.LENGTH_SHORT).show();
    }
}

